#include <iostream>
#include <cstdio>
#include <cstring>
#include <cmath>

using namespace std;

const int divisorMax = 500, PrimeMax = 1e6;

int notPrime[PrimeMax];

int main() {

    memset(notPrime, 0, sizeof(notPrime));

    for (int divisor = 2; divisor < (int)sqrt(PrimeMax); divisor++) {

    }


    return 0;
}
